# Manda App — Project Context (legacy pointer)

> **Canonical agent config for Cursor is now in `.cursor/` and `AGENTS.md` at the project root.**

| What | Canonical location |
|------|-------------------|
| Always-on rules | `.cursor/rules/*.mdc` |
| File-scoped rules | `.cursor/rules/architecture.mdc` (globs) |
| Skills | `.cursor/skills/*/SKILL.md` |
| Project overview | `AGENTS.md` (this directory's parent) |

The files under `.agents/rules/` and `.agents/skills/` are **stubs** — edit the `.cursor/` copies only.

Workspace-global rules: `../../.agents/AGENTS.md`
